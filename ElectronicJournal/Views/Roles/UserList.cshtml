@*@model IEnumerable<ElectronicJournal.Models.User>

@{
    ViewData["Title"] = "Список прав користувачів";
}

<h2>Список прав користувачів</h2>

<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-default" asp-action="Edit" asp-route-userid="@user.Id">Права доступу</a>
            </td>
        </tr>
    }
</table>*@

